import React from 'react'
import PropTypes from 'prop-types'

const RelatedPodcast = ({ lists }) => {
    return <div>{}</div>
}

RelatedPodcast.propTypes = {
    lists: PropTypes.array,
}

export default RelatedPodcast
